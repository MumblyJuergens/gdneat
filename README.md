# GDNeat
A Godot NEAT extension

## Building

Built using SCons, because it's what the supplied Godot GDExtension template for CI. Personally I would rather use CMake, but this is what we get at the moment.

```
scons
```